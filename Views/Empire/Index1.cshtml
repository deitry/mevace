@model EmpireModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>Hello from our View Template!</p>

<ul>
    @for (int i = 0; i < (Model?.Id ?? 0); i++)
    {
        <li>@Model.Greeting()</li>
    }
</ul>
